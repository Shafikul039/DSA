#include <iostream>
#include <string>
using namespace std;

int main() {
    int n;
    cin>>n;
    int a[n],sortarray[n],ma=0;

    for(int i=0;i<n;i++){
        cin>>a[i];
        if(a[i]>ma)ma=a[i];
    }
    int countarray[ma+1]={0};
    for(int i=0;i<n;i++){
        countarray[a[i]]++;
    }
    for(int i=1;i<=ma;i++)countarray[i]+=countarray[i-1];
    for(int i=n-1;i>=0;i--){
        int tmp=--countarray[a[i]];
        sortarray[tmp]=a[i];
    }
    for(int i=0;i<n;i++)cout<<sortarray[i]<<" ";
}
